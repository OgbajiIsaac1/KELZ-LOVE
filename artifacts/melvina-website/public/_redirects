# API proxy — same-origin avoids cross-site cookie issues
/api/*  https://kelz-love-api.onrender.com/api/:splat  200

# SPA fallback — all routes serve index.html for client-side routing
/*  /index.html  200
